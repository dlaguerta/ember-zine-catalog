import Ember from 'ember';

export default Ember.Route.extend({
	// model: function() {
	// 	return this.store.findAll('zine');
	// }
// 	model() {
//     return Ember.RSVP.hash({
//      zines: this.store.findAll('zine'),
//      libraries: this.store.findAll('library')
//    });
// }
 });
