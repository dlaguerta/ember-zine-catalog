
import { zineContributors } from 'ember-mongo/helpers/zine-contributors';
import { module, test } from 'qunit';

module('Unit | Helper | zine contributors');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = zineContributors([42]);
  assert.ok(result);
});

